{
  "version": 2,
  "name": "quiltro",
  "builds": [
    { "src": "./out", "use": "@now/static" },
    { "src": "./lambdas/**/*.ts", "use": "@now/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/lambdas/$1" }
  ]
}
